# DO NOT commit real values — populate via CI/CD or kubectl create secret
# kubectl create secret generic precis-api-secrets --namespace=precis \
#   --from-literal=DATABASE_URL='postgresql+asyncpg://...' \
#   --from-literal=JWT_SECRET_KEY='...' \
#   --from-literal=GOOGLE_CLIENT_ID='...' \
#   --from-literal=GOOGLE_CLIENT_SECRET='...' \
#   --from-literal=AWS_ACCESS_KEY_ID='...' \
#   --from-literal=AWS_SECRET_ACCESS_KEY='...' \
#   --from-literal=STORAGE_BUCKET='precis-documents' \
#   --from-literal=STORAGE_REGION='us-east-1'
apiVersion: v1
kind: Secret
metadata:
  name: precis-api-secrets
  namespace: precis
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_ME"
  JWT_SECRET_KEY: "REPLACE_ME"
  GOOGLE_CLIENT_ID: "REPLACE_ME"
  GOOGLE_CLIENT_SECRET: "REPLACE_ME"
  AWS_ACCESS_KEY_ID: "REPLACE_ME"
  AWS_SECRET_ACCESS_KEY: "REPLACE_ME"
  STORAGE_BUCKET: "precis-documents"
