{
  "name": "@precis/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "env/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "env/bin/pytest tests/ -v"
  }
}